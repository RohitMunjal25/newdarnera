"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import { api } from "@/lib/api";
export default function Footer() { const [email, setEmail] = useState(""), [message, setMessage] = useState(""); async function subscribe(event: FormEvent) { event.preventDefault(); try { await api("/api/newsletter", { method: "POST", body: JSON.stringify({ email }) }); setEmail(""); setMessage("You’re on the list."); } catch (err) { setMessage(err instanceof Error ? err.message : "Please try again."); } } return <footer className="bg-[#302820] px-5 py-14 text-[#f5eee7] md:px-10"><div className="mx-auto grid max-w-7xl gap-10 md:grid-cols-[1.4fr_1fr_1fr]"><div><h2 className="font-serif text-3xl tracking-[.2em]">DARNERA</h2><p className="mt-4 max-w-sm text-sm leading-6 text-[#cfc0b1]">Fragrances made for the moments you want to keep close.</p></div><div><p className="text-[10px] tracking-[.22em] text-[#d9ad88]">DISCOVER</p><div className="mt-4 flex flex-col gap-2 text-sm text-[#ddd1c5]"><Link href="/collection">All fragrances</Link><Link href="/collection?category=him">For him</Link><Link href="/collection?category=her">For her</Link><Link href="/collection?category=gift">Gift boxes</Link></div></div><div><p className="text-[10px] tracking-[.22em] text-[#d9ad88]">STAY IN THE LOOP</p><p className="mt-4 text-sm text-[#ddd1c5]">New releases, private offers and scent stories—sent sparingly.</p><form onSubmit={subscribe} className="mt-4 flex rounded-xl bg-[#fffaf3] p-1"><input required type="email" value={email} onChange={(event) => setEmail(event.target.value)} className="min-w-0 flex-1 bg-transparent px-3 text-sm text-[#332820] outline-none" placeholder="Your email" /><button className="rounded-lg bg-[#b48261] px-4 py-2 text-xs text-white">Join</button></form>{message && <p className="mt-2 text-xs text-[#d9c6b5]">{message}</p>}</div></div><p className="mx-auto mt-12 max-w-7xl border-t border-white/10 pt-6 text-xs text-[#b8a99a]">© 2026 Darnera. Made with intention.</p></footer>; }
