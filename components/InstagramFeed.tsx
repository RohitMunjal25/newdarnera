"use client";
import { useEffect, useState } from "react";
import { api } from "@/lib/api";
type Post = { _id:string; coverImageUrl:string; videoUrl?:string; instaLink:string };
export default function InstagramFeed() { const [posts, setPosts] = useState<Post[]>([]); useEffect(() => { api<{ posts:Post[] }>("/api/content/instagram").then((data) => setPosts(data.posts)).catch(() => undefined); }, []); if (!posts.length) return null; return <section className="bg-[#fffdfa] px-5 py-20 md:px-10"><div className="mx-auto max-w-7xl"><div className="mb-8 flex items-end justify-between"><div><p className="text-[10px] font-bold tracking-[.3em] text-[#ad8265]">BEHIND THE SCENT</p><h2 className="mt-3 font-serif text-4xl">Our Instagram</h2></div><span className="text-sm text-[#87644e]">@darnera.official</span></div><div className="grid grid-cols-2 gap-3 md:grid-cols-5">{posts.map((post) => <a key={post._id} href={post.instaLink} target="_blank" className="group relative aspect-square overflow-hidden rounded-2xl bg-[#eee4d9]">{post.videoUrl ? <video src={post.videoUrl} muted playsInline className="h-full w-full object-cover" /> : <img src={post.coverImageUrl} alt="Darnera Instagram" className="h-full w-full object-cover transition duration-500 group-hover:scale-110" />}</a>)}</div></div></section>; }
